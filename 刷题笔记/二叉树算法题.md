# 二叉树算法题

![img](https://labuladong.online/images/algo/binary-tree-summary/2.jpeg)

## 遍历二叉树的两种思路

```c++
// 遍历的思路
class Solution {

    // 记录最大深度
    int res = 0;
    
    // 记录遍历到的节点的深度
    int depth = 0;

public:
    int maxDepth(TreeNode* root) {
        traverse(root);
        return res;
    }

    // 遍历二叉树
    void traverse(TreeNode* root) {
        if (root == nullptr) {
            return;
        }
    
        // 前序遍历位置（进入节点）增加深度
        depth++;
        // 遍历到叶子节点时记录最大深度
        if (root->left == nullptr && root->right == nullptr) {
            res = max(res, depth);
        }
        traverse(root->left);
        traverse(root->right);
    
        // 后序遍历位置（离开节点）减少深度
        depth--;
    }

};
```

```c++
// 分解问题的思路
class Solution {
public:
    // 定义：输入一个节点，返回以该节点为根的二叉树的最大深度
    int maxDepth(TreeNode* root) {
        if (root == nullptr) {
            return 0;
        }
        // 利用定义，计算左右子树的最大深度
        int leftMax = maxDepth(root->left);
        int rightMax = maxDepth(root->right);

        // 根据左右子树的最大深度推出原二叉树的最大深度
        // 整棵树的最大深度等于左右子树的最大深度取最大值，
        // 然后再加上根节点自己
        return 1 + max(leftMax, rightMax);
    }
};
```

*二叉树解题的思维模式分两类：*

**1、是否可以通过遍历一遍二叉树得到答案**？如果可以，用一个 `traverse` 函数配合外部变量来实现，这叫「遍历」的思维模式。

**2、是否可以定义一个递归函数，通过子问题（子树）的答案推导出原问题的答案**？如果可以，写出这个递归函数的定义，并充分利用这个函数的返回值，这叫「分解问题」的思维模式。

无论使用哪种思维模式，你都需要思考：

**如果单独抽出一个二叉树节点，它需要做什么事情？需要在什么时候（前/中/后序位置）做**？其他的节点不用你操心，递归函数会帮你在所有节点上执行相同的操作。

你现在看到的这两种写法，其实不是“代码写法不同”，而是：

```text
两种完全不同的思维模型
```

这是二叉树最核心的一课，我帮你把它讲透。

------

# 一、先给你一句总结（最重要）

```text
第一种：我在“遍历整棵树，顺便算答案”（过程驱动）

第二种：我在“定义问题，用子问题推答案”（结果驱动）
```

------

# 二、第一种：遍历思路（DFS / 前序 + 回溯）

你这段代码本质是：

```text
我把所有路径走一遍，在过程中更新答案
```

------

## 核心特点

```text
1 不关心返回值
2 用全局变量 res
3 一边走一边更新
```

------

## 思维方式（非常关键）

你脑子里应该是这样：

```text
从根出发
一路往下走
记录当前深度
走到叶子就更新答案
```

👉 本质：

```text
枚举所有路径
```

------

## 类比

```text
在迷宫里找最长路径
```

你会：

```text
走一条路 → 记录长度
再走另一条路
```

------

# 三、第二种：分解问题思路（递归定义 / 后序）

这段代码本质是：

```text
我不去遍历路径
我直接问：这棵树的答案是多少？
```

------

## 核心特点

```text
1 有返回值
2 没有全局变量
3 当前结果 = 子树结果组合
```

------

## 思维方式（关键）

```text
maxDepth(root)
= max(左子树深度, 右子树深度) + 1
```

👉 本质：

```text
用子问题解决大问题
```

------

## 类比

```text
公司结构：

我不管每个人怎么干活
我只问：
左部门最大层级是多少？
右部门最大层级是多少？
```

------

# 四、最核心区别（必须搞清）

| 维度           | 遍历思路     | 分解问题思路 |
| -------------- | ------------ | ------------ |
| 思维方式       | 从上往下走   | 从下往上算   |
| 是否用返回值   | 不用         | 必须用       |
| 是否用全局变量 | 常用         | 不用         |
| 本质           | 枚举所有路径 | 递归定义     |
| 递归类型       | 前序 + 回溯  | 后序         |

------

# 五、你为什么会觉得第二种更“奇怪”

因为它违反直觉：

```text
我还没算，怎么就知道子树结果？
```

但递归的核心是：

```text
我“假设”子树已经算好了
```

------

# 六、你现在真正要建立的能力

不是记代码，而是：

```text
看到题 → 判断用哪种思维
```

------

# 七、怎么选？（面试最实用）

------

## 用遍历（第一种）：

当你：

```text
需要“走过程”
需要记录路径
需要回溯
```

比如：

- 路径问题
- 全排列
- 树路径和

------

## 用分解（第二种）：

当你：

```text
只关心“结果”
可以由子树推出来
```

比如：

- 最大深度
- 是否平衡
- 最大路径和

------

# 八、这道题为什么两种都能做？

因为：

```text
最大深度既可以：
1 枚举路径（第一种）
2 递归定义（第二种）
```

------

# 九、但面试更推荐哪种？

👉 第二种（分解）

因为：

```text
更简洁
更稳定
不容易错
没有全局变量
```

------

# 十、你可以这样记（非常关键）

```text
遍历 = 我走一遍树，顺便算答案
分解 = 我定义答案，用子树推
```

------

# 十一、一句话点醒你

```text
第一种在“干活”
第二种在“做数学”
```

------

# 十二、给你一个进阶理解（很重要）

以后你可以这样训练自己：

看到题先问：

```text
我能不能写出：
f(root) = ?
```

如果能：

👉 用第二种（分解）

如果不能：

👉 用第一种（遍历）

------

# 最后总结（压缩版）

```text
遍历：从上往下，枚举所有情况
分解：从下往上，用子树结果推
```

### 比较有感觉的题目

[114. 二叉树展开为链表](https://leetcode.cn/problems/flatten-binary-tree-to-linked-list/)

给你二叉树的根结点 `root` ，请你将它展开为一个单链表：

- 展开后的单链表应该同样使用 `TreeNode` ，其中 `right` 子指针指向链表中下一个结点，而左子指针始终为 `null` 。
- 展开后的单链表应该与二叉树 [**先序遍历**](https://baike.baidu.com/item/先序遍历/6442839?fr=aladdin) 顺序相同。

```c++
class Solution {
public:
    void flatten(TreeNode* root) {
        traverse(root);
    }
    void traverse(TreeNode* root)
    {
        if(root==nullptr) return;
        TreeNode* temp = root->right;
        root->right=root->left;
        root->left=nullptr;
        if(temp==nullptr) return;
        while(temp->right!=nullptr)
        {
            temp=temp->right;
        }
        temp->right=root->right;
        traverse(root->right);
    }
};
```

你这段代码属于**思路方向是对的，但实现逻辑是错的**，而且错在一个很隐蔽但致命的点。我直接帮你拆。

##### GPT分析

# 一、先说结论（核心 bug）

```text
你把“原右子树”接错位置了，而且顺序也错了
```

具体错误在这里：

```cpp
TreeNode* temp = root->right;
root->right = root->left;
...
temp->right = root->right; ❌
```

------

# 二、问题本质是什么

这题（展开二叉树）本质是：

👉 Flatten Binary Tree to Linked List

目标：

```text
1
 \
  2
   \
    3
```

顺序必须是：

```text
先左子树 → 再右子树
```

------

# 三、你代码的问题一步一步分析

我们用一个简单例子：

```text
    1
   / \
  2   5
 /
3
```

------

## 你的操作：

### 第一步

```cpp
temp = root->right;   // temp = 5
root->right = root->left; // right = 2
root->left = nullptr;
```

变成：

```text
1
 \
  2
 /
3
```

（5 被存到 temp）

------

### 第二步（错误关键）

```cpp
while(temp->right != nullptr)
    temp = temp->right;
```

👉 你在找：

```text
原右子树的最右节点 ❌
```

但你应该找的是：

```text
新右子树（原左子树）的最右节点 ✅
```

------

### 第三步

```cpp
temp->right = root->right;
```

👉 你做的是：

```text
5 → 2 ❌
```

但正确应该是：

```text
2 的最右节点 → 5
```

------

# 四、正确思路（核心）

你应该：

```text
1 把左子树搬到右边
2 找“新右子树”的最右节点
3 把原右子树接过去
```

------

# 五、正确代码（基于你思路改）

```cpp
void traverse(TreeNode* root)
{
    if(root == nullptr) return;

    // 先递归处理左右子树（关键！）
    traverse(root->left);
    traverse(root->right);

    TreeNode* left = root->left;
    TreeNode* right = root->right;

    root->left = nullptr;
    root->right = left;

    // 找左子树的最右节点
    TreeNode* p = root;
    while(p->right != nullptr)
    {
        p = p->right;
    }

    // 接上原右子树
    p->right = right;
}
```

------

# 六、你原代码的两个核心错误

------

## ❌ 错误1：找错“尾巴”

```text
你找的是：原右子树的尾
应该找的是：左子树的尾
```

------

## ❌ 错误2：顺序错误（致命）

你是：

```cpp
先改结构 → 再递归
```

但正确是：

```text
先递归（保证子树已经扁平）
再重组
```

------

# 七、为什么必须“先递归”

因为你要保证：

```text
左子树已经是链表
右子树已经是链表
```

否则：

```text
你接上去之后，下面结构还是乱的 ❌
```

------

# 八、最优写法（推荐你背这个）

```cpp
void flatten(TreeNode* root)
{
    if(root == nullptr) return;

    flatten(root->left);
    flatten(root->right);

    TreeNode* left = root->left;
    TreeNode* right = root->right;

    root->left = nullptr;
    root->right = left;

    TreeNode* p = root;
    while(p->right != nullptr)
        p = p->right;

    p->right = right;
}
```

------

# 九、一句话总结你这题的问题

```text
你在“还没整理好子树”之前就开始拼接结构了
```

------

# 十、帮你提炼一个通用规律（非常重要）

以后遇到“重组树结构”的题：

```text
如果依赖子结构 → 必须后序遍历
```

------

# 十一、你现在的阶段问题

你已经能：

```text
✔ 写递归
✔ 写 DFS
```

但卡在：

```text
❌ 什么时候前序
❌ 什么时候后序
❌ 什么时候可以改结构
```

------

# 十二、给你一个秒判断规则

```text
如果你要“用子树结果” → 后序
如果你要“从上往下控制” → 前序
```

----

## TreeNode* p = root;这里为什么要新建一个p
你这两个问题其实都问到了**本质层面**，我分开讲，但会帮你串起来理解。

------

# 一、为什么要 `TreeNode* p = root;`

你问的是：

```cpp
TreeNode* p = root;
```

为什么不直接用 `root`？

------

## 结论先说

```text
因为你要“遍历”，但又不能把 root 本身改掉
```

------

## 如果你不用 p，会发生什么？

假设你这样写：

```cpp
while(root->right != nullptr)
{
    root = root->right;
}
root->right = right;
```

问题来了：

```text
root 被你一路改到最右节点了 ❌
```

原来的：

```text
root = 当前子树的根
```

变成：

```text
root = 最右节点 ❌
```

------

## 这会导致什么？

```text
你丢失了原来的 root 指针
```

后面递归或结构都会错。

------

## 用 p 的本质

```cpp
TreeNode* p = root;
```

相当于：

```text
我复制一个“游标”去走链表
```

👉

```text
p 可以随便动
root 不能动
```

------

## 一句话总结

```text
p 是用来“遍历”的临时指针
root 是“结构核心”，不能乱改
```

------

# 二、你总结的递归模板，对，但还不完整

你说：

```text
1. 中断条件
2. 递归左右子树
3. 对左右子树操作
```

👉 这个是 **后序遍历模板（分治型）**

但其实二叉树递归有 **三种模式**。

------

# 三、二叉树递归三大模板（非常重要）

------

## ① 前序（从上往下控制）

```cpp
void dfs(root)
{
    if(root == nullptr) return;

    // ⭐ 在这里操作（进入节点）
    
    dfs(root->left);
    dfs(root->right);
}
```

适用于：

```text
路径问题
深度记录
回溯
```

------

## ② 后序（用子树结果）

```cpp
int dfs(root)
{
    if(root == nullptr) return ...;

    int left = dfs(root->left);
    int right = dfs(root->right);

    // ⭐ 在这里操作（利用子树结果）
    return ...
}
```

适用于：

```text
最大深度
是否平衡
flatten（你这题）
```

------

## ③ 分治（返回结构）

```cpp
TreeNode* dfs(root)
{
    if(root == nullptr) return nullptr;

    TreeNode* left = dfs(root->left);
    TreeNode* right = dfs(root->right);

    // ⭐ 重建结构
    return 新节点;
}
```

适用于：

```text
翻转二叉树
构建树
```

------

# 四、你这题为什么必须用“后序”

这题是：

👉 **先把子树变成链表，再拼接**

所以：

```text
必须先处理子树 → 再处理当前节点
```

👉 典型后序

------

# 五、你现在的理解升级一下

你原来的理解：

```text
递归 = 左右子树 + 操作
```

升级成：

```text
递归 = 选择“操作时机”
```

------

# 六、关键判断口诀（面试直接用）

```text
要不要依赖子树结果？
→ 要 → 后序
→ 不要 → 前序
```

------

# 七、再帮你把两个问题连起来

你这题：

```cpp
TreeNode* p = root;
while(p->right != nullptr)
    p = p->right;
```

其实是在做：

```text
找到“当前已经处理好的链表”的尾巴
```

而这个“已经处理好”：

```text
正是因为你用了后序递归
```

------

# 八、最终总结（帮你压缩）

### 关于 `p`

```text
p = 游标（可以动）
root = 根（不能动）
```

------

### 关于递归模板

```text
前序：从上往下控制
后序：从下往上合并
```

------

# 九、一句话帮你彻底打通

```text
你不是在“写递归代码”，你是在决定“什么时候动手改树”
```

### [116. 填充每个节点的下一个右侧节点指针](https://leetcode.cn/problems/populating-next-right-pointers-in-each-node/)

给定一个 **完美二叉树** ，其所有叶子节点都在同一层，每个父节点都有两个子节点。二叉树定义如下：

```
struct Node {
  int val;
  Node *left;
  Node *right;
  Node *next;
}
```

填充它的每个 next 指针，让这个指针指向其下一个右侧节点。如果找不到下一个右侧节点，则将 next 指针设置为 `NULL`。

初始状态下，所有 next 指针都被设置为 `NULL`。

 

**示例 1：**

![img](https://assets.leetcode.com/uploads/2019/02/14/116_sample.png)

```
输入：root = [1,2,3,4,5,6,7]
输出：[1,#,2,3,#,4,5,6,7,#]
解释：给定二叉树如图 A 所示，你的函数应该填充它的每个 next 指针，以指向其下一个右侧节点，如图 B 所示。序列化的输出按层序遍历排列，同一层节点由 next 指针连接，'#' 标志着每一层的结束。
```



**示例 2:**

```
输入：root = []
输出：[]
```

## 为什么递归要有两个参数？

你这个问题其实是关键中的关键：

> ❓ 为什么这种“两个节点一起递归”的思路能想出来？

我直接讲本质，不绕弯子。

------

# 一、你现在的问题本质

你现在的思维是：

```text
树题 = dfs(root)
```

也就是：

```text
一个节点 → 做事情
```

但这题是：

```text
节点之间的关系
```

👉 **这就是卡住你的核心原因**

------

# 二、一个通用思考套路（非常重要）

以后遇到树题，先问自己三件事：

------

## ① 我要处理的“对象”是什么？

- 最大深度 👉 一个节点
- 翻转二叉树 👉 一个节点
- 这题 👉 ❗两个节点之间的关系

------

## ② 如果是“关系问题”，怎么办？

直接升级函数参数：

```cpp
dfs(node1, node2)
```

------

## ③ 定义函数含义（最关键）

这题你要定义：

```text
dfs(node1, node2) = 把这两个节点连接起来
```

------

# 三、为什么这样一定能做出来

因为递归本质是：

```text
大问题 = 小问题重复
```

这题的大问题是：

```text
把整棵树的 next 连好
```

你拆成小问题：

```text
把两个相邻节点连好
```

------

# 四、如何一步一步“推”出来（重点）

我们模拟你现场思考：

------

## 第一步：最简单情况

```text
    1
   / \
  2   3
```

你肯定能想到：

```cpp
2->next = 3;
```

------

## 第二步：扩展到下一层

```text
4 5 6 7
```

你会发现：

```text
4 -> 5   ✔
6 -> 7   ✔
5 -> 6   ❗不会
```

👉 卡住点出现了

------

## 第三步：解决“5 和 6”

这时候你就应该意识到：

```text
我需要同时知道 5 和 6
```

而 5 和 6 来自：

```text
node1->right
node2->left
```

👉 所以你需要函数：

```cpp
dfs(node1, node2)
```

------

# 五、关键突破点（面试最值钱）

```text
当一个节点不够用 → 就传多个节点
```

这是所有“树进阶题”的核心套路。

------

# 六、总结成一个通用模板

以后遇到类似题：

------

## Step 1：看问题类型

```text
是节点问题？
还是节点关系问题？
```

------

## Step 2：定义函数

```cpp
节点问题：dfs(root)

关系问题：dfs(a, b)
```

------

## Step 3：写递归

这题就是：

```cpp
// 连接当前
a->next = b;

// 处理子问题
dfs(a->left, a->right);
dfs(b->left, b->right);
dfs(a->right, b->left); // 关键
```

------

# 七、你现在缺的不是知识，是“模式识别”

这类题本质是一个套路：

```text
树 + 关系 → 多参数递归
```

类似的题还有：

- 判断对称树（左 vs 右）
- 判断两棵树是否相同
- Lowest Common Ancestor（有点变种）

------

# 八、给你一个“秒判断口诀”

以后看到树题：

```text
如果需要比较 / 连接 / 同步两个节点
→ 用 dfs(node1, node2)
```

------

